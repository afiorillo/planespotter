# Copy to .env (gitignored) and fill in your secrets.
# AeroDataBox RapidAPI key — enables origin/destination + delay enrichment.
# Leave empty to run with adsbdb route-only (no delay).
AERODATABOX_API_KEY=""
